<!-- Panneau d'administration des logements --> 
<div class="col-12">
    <h1 class="my-5 text-center">Panneau d'administration des logements</h1>
</div>

<!-- Lien vers le formulaire de création de logement -->
<div class="col-12 mb-2">
    <a href="add_logement.php" class="btn btn-primary">
        <svg class="mr-1" width="1em" height="1em" viewBox="0 0 16 16" class="bi bi-plus-circle-fill" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
            <path fill-rule="evenodd" d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zM8.5 4.5a.5.5 0 0 0-1 0v3h-3a.5.5 0 0 0 0 1h3v3a.5.5 0 0 0 1 0v-3h3a.5.5 0 0 0 0-1h-3v-3z"/>
        </svg>
        Ajouter un logement
    </a>
</div>

<!-- Liste des logements --> 
<section class="col mb-5">

    <!-- S'il n'y a pas de logement -->
    <?php if(empty($logements)): ?>
        <p class="text-center">Pas de logement pour le moment ! </p>
        
    <?php else: ?>
        <table class="table table-striped">
            <thead class="thead-dark">
                <tr>
                    <th scope="col">#ID</th>
                    <th scope="col">Titre</th>
                    <th scope="col">Adresse</th>
                    <th scope="col">Ville</th>
                    <th scope="col">CP</th>
                    <th scope="col">Surface</th>
                    <th scope="col">Prix</th>
                    <th scope="col">Photo</th>
                    <th scope="col">Type</th>
                    <th scope="col">Description</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach($logements as $logement): ?>
                    <tr>
                        <th scope="row">
                            <a class="text-body" href="logement_details.php?id=<?=$logement['id_logement'];?>">
                                <?=$logement['id_logement'];?>
                            </a>
                        </th>
                        <td><?=$logement['titre'];?></td>
                        <td><?=$logement['adresse'];?></td>
                        <td><?=$logement['ville'];?></td>
                        <td><?=$logement['cp'];?></td>
                        <td><?=$logement['surface'];?> m²</td>
                        <td><?=$logement['prix'];?> €</td>
                        <td class="text-center">
                            <img class="w-75" src="images/<?=$logement['picture_1'];?>" alt="<?=$logement['titre'];?>">
                        </td>
                        <td><?=$logement['label'];?></td>
                        <td><?=substr($logement['description'], 0, 30) . "...";?></td>
                    </tr>
                <?php endforeach;?>
            </tbody>
        </table>
    <?php endif;?>
</section>